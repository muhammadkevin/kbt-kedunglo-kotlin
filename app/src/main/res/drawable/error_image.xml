<!-- res/drawable/error_image.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background color for error image (optional) -->
    <item android:drawable="@android:color/holo_red_light" />

    <!-- Error image or icon (adjust width and height as needed) -->
    <item
        android:drawable="@drawable/ic_error"
        android:gravity="center"
        android:width="48dp"
        android:height="48dp" />

</layer-list>
